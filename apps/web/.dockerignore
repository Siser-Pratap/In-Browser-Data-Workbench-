# Everything here is either rebuilt in the image or platform-specific to the
# host — copying it in would be slower and, for node_modules, actively wrong
# (Next's SWC binary is compiled per-platform).
node_modules
.next
out
coverage
test-results
playwright-report

# Regenerated by scripts/copy-duckdb.mjs during the build.
public/duckdb

.git
.env
.env.*
!.env.example
*.tsbuildinfo
.DS_Store
